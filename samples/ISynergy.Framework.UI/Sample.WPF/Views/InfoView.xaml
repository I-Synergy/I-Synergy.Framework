<c:View xmlns:c="clr-namespace:ISynergy.Framework.UI.Controls;assembly=ISynergy.Framework.UI.Wpf" x:Class="Sample.Views.InfoView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="InfoView">

    <Grid>
        <StackPanel Orientation="Vertical">
            <TextBlock Text="Company name:"/>
            <TextBox Tag="Company name" Text="{Binding CompanyName}" IsReadOnly="True"/>
            <TextBlock Text="Product name:"/>
            <TextBox Tag="Product name" Text="{Binding ProductName}" IsReadOnly="True"/>
            <TextBlock Text="Version:"/>
            <TextBox Tag="Version" Text="{Binding Version}" IsReadOnly="True"/>
            <TextBlock Text="Copyrights:"/>
            <TextBox Tag="Copyrights" Text="{Binding Copyrights}" IsReadOnly="True"/>

            <TextBlock Text="&#xf53f;" FontFamily="{StaticResource FontAwesome}" FontSize="36" Margin="4"/>
        </StackPanel>

        <Button Content="Set Busy On" Command="{Binding BusyOnCommand}" Margin="2" Width="125" Height="35" VerticalAlignment="Bottom" HorizontalAlignment="Center"/>
    </Grid>
</c:View>
