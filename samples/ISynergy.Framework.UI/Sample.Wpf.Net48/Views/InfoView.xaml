<c:View xmlns:c="clr-namespace:ISynergy.Framework.UI.Controls;assembly=ISynergy.Framework.UI.Wpf" x:Class="Sample.Views.InfoView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="InfoView">

    <Grid>
        <StackPanel Orientation="Vertical">
            <TextBox Tag="Company name" Text="{Binding SelectedItem.CompanyName, Mode=OneWay}" IsReadOnly="True" Margin="2"/>
            <TextBox Tag="Product name" Text="{Binding SelectedItem.ProductName, Mode=OneWay}" IsReadOnly="True" Margin="2"/>
            <TextBox Tag="Version" Text="{Binding SelectedItem.Version, Mode=OneWay}" IsReadOnly="True" Margin="2"/>
            <TextBox Tag="Copyrights" Text="{Binding SelectedItem.Copyrights, Mode=OneWay}" IsReadOnly="True" Margin="2"/>
        </StackPanel>
    </Grid>
</c:View>
