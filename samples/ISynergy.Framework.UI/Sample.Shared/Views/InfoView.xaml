<c:View xmlns:c="using:ISynergy.Framework.UI.Controls" 
    x:Class="Sample.Views.InfoView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <StackPanel Orientation="Vertical">
            <TextBox Header="Company name" Text="{Binding CompanyName}" IsReadOnly="True"/>
            <TextBox Header="Product name" Text="{Binding ProductName}" IsReadOnly="True"/>
            <TextBox Header="Version" Text="{Binding Version}" IsReadOnly="True"/>
            <TextBox Header="Copyrights" Text="{Binding Copyrights}" IsReadOnly="True"/>
        </StackPanel>
    </Grid>
</c:View>
