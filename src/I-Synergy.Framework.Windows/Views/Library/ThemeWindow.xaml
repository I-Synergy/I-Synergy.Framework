<w:Window xmlns:w="using:ISynergy.Controls"
    x:Class="ISynergy.Views.Library.ThemeWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ISynergy.Views.Library"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:Converters="using:ISynergy.Converters"
    mc:Ignorable="d"
    Title="{Binding Title}"
    PrimaryButtonCommand="{Binding Close_Command}"
    PrimaryButtonStyle="{ThemeResource DefaultButtonStyle}"
    SecondaryButtonStyle="{ThemeResource DefaultButtonStyle}">

    <w:Window.Resources>
        <Converters:StringToBooleanConverter x:Key="StringToBooleanConverter" />
        <Converters:SolidColorBrushToHexStringConverter x:Key="SolidColorBrushToHexStringConverter"/>
    </w:Window.Resources>
    
    <StackPanel Orientation="Vertical">
        <StackPanel Orientation="Horizontal">
            <RadioButton x:Name="Button_Color_1" Command="{Binding Color_Command}" CommandParameter="{Binding Background, ElementName=Color_1, Converter={StaticResource SolidColorBrushToHexStringConverter}}" GroupName="Color" MinWidth="0" IsChecked="True" />
            <Border x:Name="Color_1" Height="32" Width="32" CornerRadius="16" Background="#FF3399FF" Margin="2" />
        </StackPanel>

        <StackPanel Orientation="Horizontal">
            <RadioButton x:Name="Button_Color_2" Command="{Binding Color_Command}" CommandParameter="{Binding Background, ElementName=Color_2, Converter={StaticResource SolidColorBrushToHexStringConverter}}" GroupName="Color" MinWidth="0" />
            <Border x:Name="Color_2" Height="32" Width="32" CornerRadius="16" Background="#FF2672EC" Margin="2" />
        </StackPanel>

        <StackPanel Orientation="Horizontal">
            <RadioButton x:Name="Button_Color_3" Command="{Binding Color_Command}" CommandParameter="{Binding Background, ElementName=Color_3, Converter={StaticResource SolidColorBrushToHexStringConverter}}" GroupName="Color" MinWidth="0"/>
            <Border x:Name="Color_3" Height="32" Width="32" CornerRadius="16" Background="#FFAC193D" Margin="2" />
        </StackPanel>

        <StackPanel Orientation="Horizontal">
            <RadioButton x:Name="Button_Color_4" Command="{Binding Color_Command}" CommandParameter="{Binding Background, ElementName=Color_4, Converter={StaticResource SolidColorBrushToHexStringConverter}}" GroupName="Color" MinWidth="0"/>
            <Border x:Name="Color_4" Height="32" Width="32" CornerRadius="16" Background="#FFD24726" Margin="2" />
        </StackPanel>

        <StackPanel Orientation="Horizontal">
            <RadioButton x:Name="Button_Color_5" Command="{Binding Color_Command}" CommandParameter="{Binding Background, ElementName=Color_5, Converter={StaticResource SolidColorBrushToHexStringConverter}}" GroupName="Color" MinWidth="0"/>
            <Border x:Name="Color_5" Height="32" Width="32" CornerRadius="16" Background="#FFF3B200" Margin="2" />
        </StackPanel>

        <StackPanel Orientation="Horizontal">
            <RadioButton x:Name="Button_Color_6" Command="{Binding Color_Command}" CommandParameter="{Binding Background, ElementName=Color_6, Converter={StaticResource SolidColorBrushToHexStringConverter}}" GroupName="Color" MinWidth="0"/>
            <Border x:Name="Color_6" Height="32" Width="32" CornerRadius="16" Background="#FF77B900" Margin="2" />
        </StackPanel>
    </StackPanel>
</w:Window>
