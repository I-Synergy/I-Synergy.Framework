<w:Window xmlns:w="using:ISynergy.Controls"
    x:Class="ISynergy.Views.Library.MapsWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:maps="using:Windows.UI.Xaml.Controls.Maps"
    mc:Ignorable="d" 
    Width="1024" 
    Height="{StaticResource ContentDialogMaxHeight}"
    Title="{Binding Title}"
    IsEnabled="{Binding ISynergyService.BusyService.IsEnabled}"
    Template="{StaticResource WindowTemplate}">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <maps:MapControl x:Name="BingMaps" ZoomLevel="15"
                         ZoomInteractionMode="GestureAndControl"
                         TiltInteractionMode="GestureAndControl"   
                         MapServiceToken="{Binding Token, Mode=OneWay}" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" />

        <Grid Grid.Row="1" Margin="0,10,0,0" >
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Orientation="Vertical">
                <TextBlock FontSize="{StaticResource MediumFontSize}">
                    <TextBlock.Inlines>
                        <Run Text="Total estimated time (minutes): "/>
                        <Run Text="{Binding TimeToArrival, Mode=OneWay}"/>
                    </TextBlock.Inlines>
                </TextBlock>
                <TextBlock FontSize="{StaticResource MediumFontSize}">
                    <TextBlock.Inlines>
                        <Run Text="Total length (kilometers): "/>
                        <Run Text="{Binding DistcanceToArrival, Mode=OneWay}"/>
                    </TextBlock.Inlines>
                </TextBlock>
            </StackPanel>

            <Button x:Name="Button_Close" Grid.Column="1" Content="{CustomResource Generic_Close}" Command="{Binding Close_Command}" />
        </Grid>

        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <ProgressRing x:Name="IsBusyProgressRing" IsActive="{Binding ISynergyService.BusyService.IsBusy}" Height="36" Width="36" />
            <TextBlock FontSize="16" Text="{Binding ISynergyService.BusyService.BusyMessage}" Foreground="{Binding Foreground, ElementName=IsBusyProgressRing}" Visibility="{Binding ISynergyService.BusyService.IsBusy}" />
        </StackPanel>
    </Grid>
</w:Window>
