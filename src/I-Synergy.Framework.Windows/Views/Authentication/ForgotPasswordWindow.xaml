<w:Window xmlns:w="using:ISynergy.Controls"
    x:Class="ISynergy.Views.Authentication.ForgotPasswordWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:Converters="using:ISynergy.Converters"
    mc:Ignorable="d"
    Title="{Binding Title}"
    PrimaryButtonCommand="{Binding Submit_Command}"
    SecondaryButtonCommand="{Binding Close_Command}"
    Width="500"
    PrimaryButtonStyle="{StaticResource DefaultButtonStyle}"
    SecondaryButtonStyle="{StaticResource DefaultButtonStyle}">

    <w:Window.Resources>
        <Converters:BooleanToStringConverter x:Key="BooleanToStringConverter" True="True" False="False" />
    </w:Window.Resources>
    
    <Grid Margin="10">
        <TextBox x:Name="TextBox_EmailAddress" Header="{CustomResource Generic_Email}" Text="{Binding EmailAddress, Mode=TwoWay}" InputScope="EmailNameOrAddress"/>
    </Grid>
</w:Window>
