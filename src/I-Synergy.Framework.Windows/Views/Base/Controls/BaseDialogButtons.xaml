<UserControl
    x:Class="ISynergy.Views.Base.Controls.BaseDialogButtons"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="{ThemeResource ContentDialogButtonMinHeight}"
    d:DesignWidth="400">

    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
        <Button x:Name="ButtonSubmit"
                        Content="{CustomResource Generic_Ok}"
                        Command="{Binding Submit_Command}"
                        CommandParameter="{Binding SelectedItem}"
                        Margin="{ThemeResource ContentDialogButton1HostMargin}"
                        MinWidth="{ThemeResource ContentDialogButtonMinWidth}"
                        MinHeight="{ThemeResource ContentDialogButtonMinHeight}"
                        MaxWidth="{ThemeResource ContentDialogButtonMaxWidth}" />
        <Button x:Name="ButtonCancel"
                        Content="{CustomResource Generic_Cancel}"
                        Command="{Binding Close_Command}"
                        Margin="{ThemeResource ContentDialogButton2HostMargin}"
                        MinWidth="{ThemeResource ContentDialogButtonMinWidth}"
                        MinHeight="{ThemeResource ContentDialogButtonMinHeight}"
                        MaxWidth="{ThemeResource ContentDialogButtonMaxWidth}" />
    </StackPanel>
</UserControl>
