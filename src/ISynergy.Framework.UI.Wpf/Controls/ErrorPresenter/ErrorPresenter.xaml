<UserControl x:Class="ISynergy.Framework.UI.Controls.ErrorPresenter"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:Converters="clr-namespace:ISynergy.Framework.UI.Converters"
    mc:Ignorable="d"
    HorizontalAlignment="Stretch" 
    VerticalAlignment="Top">

    <UserControl.Resources>
        <Converters:BooleanToVisibilityConverter x:Key="ErrorPresenterConverter" True="Collapsed" False="Visible"/>
    </UserControl.Resources>
    <UserControl.Visibility>
        <Binding Converter="{StaticResource ErrorPresenterConverter}" Path="IsValid"/>
    </UserControl.Visibility>

    <Border x:Name="RootBorder" CornerRadius="4" Background="Red">
        <Grid x:Name="RootGrid" Margin="4">
            <ItemsControl ItemsSource="{Binding Errors}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding}" Foreground="White" FontSize="12" FontWeight="Bold" />
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Grid>
    </Border>
</UserControl>
