<?xml version="1.0" encoding="utf-8" ?>
<c:Window
    x:Class="ISynergy.Framework.UI.Windows.ThemeWindow"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:c="clr-namespace:ISynergy.Framework.UI.Controls"
    xmlns:converters="clr-namespace:ISynergy.Framework.UI.Converters;assembly=ISynergy.Framework.UI.Maui"
    xmlns:m="clr-namespace:ISynergy.Framework.UI.Markup"
    xmlns:viewmodels="clr-namespace:ISynergy.Framework.UI.ViewModels;assembly=ISynergy.Framework.UI"
    Title="{Binding Title}"
    x:DataType="viewmodels:ThemeViewModel">

    <c:Window.Resources>
        <converters:StringToColorBrushConverter x:Key="StringToColorBrushConverter" />
    </c:Window.Resources>

    <Grid>
        <ContentView
            ControlTemplate="{DynamicResource DefaultDialogTemplate}"
            HeightRequest="600"
            WidthRequest="400">

            <VerticalStackLayout Spacing="12">
                <!-- Theme Selection (System, Light, Dark) -->
                <!-- Disable compiled bindings to work around .NET 10 MAUI source generator bug (CS8820) -->
                <!-- The bug occurs when binding to nullable paths like SelectedItem.Theme -->
                <Grid HorizontalOptions="Center" x:DataType="{x:Null}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition />
                        <ColumnDefinition />
                        <ColumnDefinition />
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <RadioButton
                        Content="{m:GetString System}"
                        Grid.Column="1"
                        HorizontalOptions="Center"
                        GroupName="Theme"
                        IsChecked="{Binding SelectedItem.Theme, Converter={converters:RadioButtonToThemeConverter Theme=Default}}" />
                    <RadioButton
                        Content="{m:GetString Light}"
                        Grid.Column="2"
                        HorizontalOptions="Center"
                        GroupName="Theme"
                        IsChecked="{Binding SelectedItem.Theme, Converter={converters:RadioButtonToThemeConverter Theme=Light}}" />
                    <RadioButton
                        Content="{m:GetString Dark}"
                        Grid.Column="3"
                        HorizontalOptions="Center"
                        GroupName="Theme"
                        IsChecked="{Binding SelectedItem.Theme, Converter={converters:RadioButtonToThemeConverter Theme=Dark}}" />
                </Grid>

                <!-- Color Grid -->
                <!-- Disable compiled bindings for CollectionView with nullable SelectedItem.Color binding -->
                <CollectionView
                    x:DataType="{x:Null}"
                    HorizontalOptions="Center"
                    ItemsSource="{Binding ThemeColors.Colors}"
                    SelectedItem="{Binding SelectedItem.Color, Mode=TwoWay}"
                    SelectionMode="Single">
                    <CollectionView.ItemsLayout>
                        <GridItemsLayout Orientation="Vertical" Span="8" />
                    </CollectionView.ItemsLayout>
                    <CollectionView.ItemTemplate>
                        <DataTemplate x:DataType="x:String">
                            <Grid Padding="2">
                                <VisualStateManager.VisualStateGroups>
                                    <VisualStateGroup Name="CommonStates">
                                        <VisualState Name="Normal">
                                            <VisualState.Setters>
                                                <Setter TargetName="SelectionBorder" Property="Border.IsVisible" Value="False" />
                                            </VisualState.Setters>
                                        </VisualState>
                                        <VisualState Name="Selected">
                                            <VisualState.Setters>
                                                <Setter TargetName="SelectionBorder" Property="Border.IsVisible" Value="True" />
                                            </VisualState.Setters>
                                        </VisualState>
                                    </VisualStateGroup>
                                </VisualStateManager.VisualStateGroups>

                                <!-- Color Circle -->
                                <Ellipse
                                    Fill="{Binding Converter={StaticResource StringToColorBrushConverter}}"
                                    HeightRequest="28"
                                    HorizontalOptions="Center"
                                    ToolTipProperties.Text="{Binding}"
                                    VerticalOptions="Center"
                                    WidthRequest="28" />

                                <!-- Selection Border - Controlled by VisualStateManager -->
                                <Border
                                    x:Name="SelectionBorder"
                                    Stroke="{AppThemeBinding Light={DynamicResource TintedGray900}, Dark={DynamicResource TintedGray100}}"
                                    StrokeThickness="2"
                                    BackgroundColor="Transparent"
                                    HeightRequest="32"
                                    HorizontalOptions="Center"
                                    VerticalOptions="Center"
                                    WidthRequest="32">
                                    <Border.StrokeShape>
                                        <RoundRectangle CornerRadius="16" />
                                    </Border.StrokeShape>
                                </Border>
                            </Grid>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>
            </VerticalStackLayout>
        </ContentView>
    </Grid>
</c:Window>